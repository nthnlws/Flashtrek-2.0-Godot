extends Sprite2D

func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass


func _on_player_is_warping():
	pass
	#sprite.scale *= 1.75
	#elif player.warping == true:
		#player.scale.x = Sprite2D.scale.x / 1.75
